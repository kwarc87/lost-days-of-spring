import { MESSAGES } from "../messages.js";

let _nextCheckpointPlatformId = 9001;
let _nextTeleportPlatformId = 8001;

export const GameFactory = {
    GRID: 48,
    SCALE: 3,
    player: (overrides = {}) => ({
        x: 0,
        y: 0,
        prevX: 0,
        prevY: 0,
        w: 36,
        h: 108,
        vx: 0,
        vy: 0,
        life: 6,
        maxLife: 6,
        isHit: false,
        lastHitTime: 0,
        hitCooldown: 1000,
        knockbackUntil: 0,
        knockbackControlLock: 200,
        acceleration: 0.8,
        deceleration: 1.2,
        airAcceleration: 2,
        airDeceleration: 2,
        carryVx: 0,
        carryVxInitial: 0,
        carryStartAt: 0,
        carryDuration: 400,
        speed: 5.8,
        crouchSpeed: 3,
        crouchHeight: 45,
        crouchWidth: 72,
        originalHeight: 108,
        originalWidth: 36,
        airborne: true,
        jump: 16.4,
        jumpPressedByUser: false,
        jumpPressedAt: 0,
        jumpBufferDuration: 80,
        lastGroundedAt: 0,
        coyoteDuration: 80,
        onGroundId: null,
        onGroundType: null,
        lastGroundId: null,
        lastGroundType: null,
        coinsCount: 0,
        splintersCount: 0,
        artifactsCount: 0,
        facing: "right",
        shooting: false,
        lastShootTime: 0,
        shootingOffsetY: -4,
        shootingCrouchOffsetY: 4,
        shootingOffsetX: 38,
        shootingCrouchOffsetX: 44,
        ...overrides,
    }),
    solid: ({ id, x, y, w, h, layout = "ground", color } = {}) => ({
        id,
        x,
        y,
        w,
        h,
        type: "solid",
        layout,
        ...(color !== undefined && { color }),
    }),
    ice: ({
        id,
        x,
        y,
        w,
        h,
        layout = "ice",
        acceleration = 0.05,
        deceleration = 0.025,
        airAcceleration = 0.15,
        airDeceleration = 0.075,
        color,
    } = {}) => ({
        id,
        x,
        y,
        w,
        h,
        type: "solid",
        acceleration,
        deceleration,
        airAcceleration,
        airDeceleration,
        layout,
        ...(color !== undefined && { color }),
    }),
    oneDirectionIce: ({
        id,
        x,
        y,
        w,
        h,
        layout = "iceOneDirection",
        acceleration = 0.05,
        deceleration = 0.025,
        airAcceleration = 0.15,
        airDeceleration = 0.075,
        color,
    } = {}) => ({
        id,
        x,
        y,
        w,
        h,
        type: "oneDirection",
        acceleration,
        deceleration,
        airAcceleration,
        airDeceleration,
        layout,
        ...(color !== undefined && { color }),
    }),
    oneDirection: ({
        id,
        x,
        y,
        w,
        h,
        layout = "boardOneDirection",
        ...rest
    } = {}) => ({
        id,
        x,
        y,
        w,
        h,
        type: "oneDirection",
        layout,
        ...rest,
    }),
    cracks: ({ id, x, y, w, h, layout = "cracks" } = {}) => ({
        id,
        x,
        y,
        w,
        h,
        type: "solid",
        layout,
    }),
    booster: ({ id, x, y, w, h, boostSpeed = 20 } = {}) => ({
        id,
        x,
        y,
        w,
        h,
        elasticity: 0,
        type: "booster",
        layout: "booster",
        boostSpeed,
    }),
    elevator: ({
        id,
        startX,
        startY,
        w,
        h,
        targetX,
        targetY,
        speed,
        waitTime,
        layout = "brick",
        triggered = false,
    } = {}) => {
        const dx = targetX - startX;
        const dy = targetY - startY;
        const length = Math.hypot(dx, dy);
        return {
            id,
            x: startX,
            y: startY,
            w,
            h,
            startX,
            startY,
            targetX,
            targetY,
            dirX: dx / length,
            dirY: dy / length,
            //the range that makes sense is 2-8
            speed,
            direction: 1,
            waitTime,
            idleUntil: 0,
            triggered,
            type: "elevator",
            layout,
        };
    },
    enemy: ({
        id,
        startX,
        targetX,
        startY,
        targetY,
        speed = 1.5,
        health = 15,
        ...rest
    } = {}) => {
        const dx = targetX - startX;
        const dy = targetY - startY;
        const length = Math.hypot(dx, dy);

        return {
            id,
            startX,
            targetX,
            x: startX,
            startY,
            targetY,
            y: startY,
            prevX: startX,
            prevY: startY,
            dirX: dx / length,
            dirY: dy / length,
            speed,
            direction: 1,
            vx: speed,
            health,
            recoilX: 8,
            recoilY: 8,
            isDamaged: false,
            dying: false,
            dyingStartedAtMs: null,
            dyingDurationMs: 1000,
            dead: false,
            damage: 1,
            damageTime: 0,
            wasCollidingWithPlayer: false,
            collidingWithPlayerThisFrame: false,
            mainColor: "#e84855",
            secondaryColor: "#f7b32b",
            ...rest,
        };
    },
    slime: (args = {}) => {
        const w = 86;
        const h = 128;
        const { startX, targetX, startY, targetY } = args;
        const goingLeft = startX > targetX;
        const goingRight = targetX > startX;
        const goingUp = startY > targetY;
        const goingDown = targetY > startY;
        return {
            ...GameFactory.enemy({
                ...args,
                startX: goingLeft ? startX - w : startX,
                targetX: goingRight ? targetX - w : targetX,
                startY: goingUp ? startY - h : startY,
                targetY: goingDown ? targetY - h : targetY,
            }),
            ...(goingLeft || goingUp ? { direction: -1 } : {}),
            sprite: "slime",
            w,
            h,
            offsetX: 5,
        };
    },
    evilEye: (args = {}) => {
        const w = 60;
        const h = 74;
        const { startX, targetX, startY, targetY } = args;
        const goingLeft = startX > targetX;
        const goingRight = targetX > startX;
        const goingUp = startY > targetY;
        const goingDown = targetY > startY;
        return {
            ...GameFactory.enemy({
                ...args,
                startX: goingLeft ? startX - w : startX,
                targetX: goingRight ? targetX - w : targetX,
                startY: goingUp ? startY - h : startY,
                targetY: goingDown ? targetY - h : targetY,
            }),
            ...(goingLeft || goingUp ? { direction: -1 } : {}),
            sprite: "evilEye",
            w,
            h,
            offsetX: 16,
            offsetY: 34,
        };
    },
    exit: ({ id, x, y, w, h, ...rest } = {}) => ({
        id,
        x,
        y,
        w,
        h,
        cordX: 108,
        cordY: 36,
        url: "textures/all-props.png",
        triggerMargin: GameFactory.GRID * 3,
        ...rest,
    }),
    message: ({
        id,
        x,
        y,
        w,
        h,
        offsetX = 0,
        offsetY = 0,
        title,
        lines,
        relatedTo = "viewPort",
        strategy = "single",
        displayTime = 5000,
        delay = 0,
    } = {}) => ({
        id,
        x,
        y,
        w,
        h,
        offsetX,
        offsetY,
        ...(title !== undefined && { title }),
        lines: lines ?? [],
        relatedTo,
        strategy,
        ...(displayTime !== undefined && { displayTime }),
        ...(delay > 0 && { delay }),
        shown: false,
    }),
    collectible: ({ id, x, y, ...rest } = {}) => ({
        id,
        x,
        y,
        w: 24,
        h: 24,
        collected: false,
        ...rest,
    }),
    artifact: ({ id, x, y, cordX = 0, cordY = 0, ...rest } = {}) =>
        GameFactory.collectible({
            id,
            x,
            y,
            w: 48,
            h: 48,
            url: "textures/icons.png",
            cordX: cordX * 16,
            cordY: cordY * 16,
            ...rest,
        }),
    weapon: (overrides = {}) => ({
        id: 1,
        color: "#ffc300",
        speed: 12,
        shootFrequency: 125,
        ammo: {
            vx: 0,
            vy: 0,
            w: 8,
            h: 8,
            damage: 5,
        },
        ...overrides,
    }),
    cannon: ({ id, x, y, targetY, ...rest } = {}) => ({
        id,
        x,
        y,
        w: 96,
        h: 96,
        targetY,
        color: "#35fffd",
        speed: 8,
        shootFrequency: 2000,
        delay: 0,
        lastShootTime: 0,
        ammo: {
            vx: 0,
            vy: 0,
            w: 8,
            h: 8,
            damage: 1,
            recoilX: 0,
            recoilY: 6,
        },
        ...rest,
    }),
    rowOfCollectibles: ({ startId, count, startX, y, gap } = {}) =>
        Array.from({ length: count }, (_, i) =>
            GameFactory.collectible({
                id: startId + i,
                x: startX + i * gap,
                y,
            }),
        ),
    columnOfCollectibles: ({ startId, count, x, startY, gap } = {}) =>
        Array.from({ length: count }, (_, i) =>
            GameFactory.collectible({
                id: startId + i,
                x,
                y: startY + i * gap,
            }),
        ),
    rowOfSpikes: ({
        startId,
        count,
        startX,
        y,
        position = "down",
        damage = 1,
    } = {}) => {
        return Array.from({ length: count }, (_, i) => {
            const firstItemAdditionalMargin = i === 0 ? 9 : 0;
            const lastItemAdditionalMargin =
                i === count - 1 && position === "down" ? 9 : 0;
            const spikeW = 16 * GameFactory.SCALE;
            return GameFactory.spike({
                id: startId + i,
                x: startX + i * spikeW + firstItemAdditionalMargin,
                y,
                variant: (i % 2) + 1,
                position,
                damage,
                w:
                    spikeW -
                    firstItemAdditionalMargin -
                    lastItemAdditionalMargin,
                offsetX: -firstItemAdditionalMargin,
            });
        });
    },
    spike: ({
        id,
        x,
        y,
        variant = 1,
        position = "down",
        damage = 1,
        recoilX = 4,
        recoilY = 8,
        w = 16 * GameFactory.SCALE,
        h = 16 * GameFactory.SCALE,
        offsetX = 0,
        offsetY = -3,
    } = {}) => ({
        id,
        x,
        y: position === "up" ? y : y + 9,
        w,
        h: position === "up" ? h - 18 : h - 9,
        variant,
        position,
        damage,
        recoilX,
        recoilY: position === "down" ? recoilY : 0,
        type: "spike",
        offsetX,
        offsetY,
    }),
    teleport: ({ x, y, delay, frozenDelay, ...rest } = {}) => ({
        x,
        y,
        url: "textures/tilesets.png",
        cordX: 64,
        cordY: 192,
        w: 32 * GameFactory.SCALE,
        h: 68 * GameFactory.SCALE,
        delay,
        frozenDelay,
        ...rest,
    }),
    checkpoint: ({ id, x, y, w, h, reached = false } = {}) => ({
        id,
        x,
        y,
        w,
        h,
        reached,
    }),
    environment: {
        checkpointBack: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 0,
            cordY: 260,
            w: 16 * GameFactory.SCALE,
            h: 49 * GameFactory.SCALE,
            ...rest,
        }),
        checkpointFront: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 16,
            cordY: 260,
            w: 16 * GameFactory.SCALE,
            h: 49 * GameFactory.SCALE,
            ...rest,
        }),
        terminalDirection: ({ x, y, direction = "up", ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 288,
            cordY: 192,
            w: 24 * GameFactory.SCALE,
            h: 28 * GameFactory.SCALE,
            direction,
            ...rest,
        }),
        terminal: ({ x, y, ...rest } = {}) =>
            GameFactory.environment.terminalDirection({ x, y, ...rest }),
        plant001: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 48,
            cordY: 98,
            w: 32 * GameFactory.SCALE,
            h: 30 * GameFactory.SCALE,
            ...rest,
        }),
        plant002: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 16,
            cordY: 100,
            w: 32 * GameFactory.SCALE,
            h: 32 * GameFactory.SCALE,
            ...rest,
        }),
        plant003: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 96,
            cordY: 105,
            w: 16 * GameFactory.SCALE,
            h: 21 * GameFactory.SCALE,
            ...rest,
        }),
        plant004: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 128,
            cordY: 104,
            w: 16 * GameFactory.SCALE,
            h: 22 * GameFactory.SCALE,
            ...rest,
        }),
        plant005: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/all-props.png",
            cordX: 366,
            cordY: 112,
            w: 32 * GameFactory.SCALE,
            h: 32 * GameFactory.SCALE,
            ...rest,
        }),
        plant006: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/all-props.png",
            cordX: 446,
            cordY: 112,
            w: 32 * GameFactory.SCALE,
            h: 32 * GameFactory.SCALE,
            ...rest,
        }),
        plant007: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 128,
            cordY: 260,
            w: 32 * GameFactory.SCALE,
            h: 32 * GameFactory.SCALE,
            ...rest,
        }),
        plant008: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 176,
            cordY: 260,
            w: 25 * GameFactory.SCALE,
            h: 18 * GameFactory.SCALE,
            ...rest,
        }),
        wallPlant001: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 130,
            cordY: 48,
            w: 16 * GameFactory.SCALE,
            h: 27 * GameFactory.SCALE,
            ...rest,
        }),
        plate001: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 160,
            cordY: 107,
            w: 32 * GameFactory.SCALE,
            h: 21 * GameFactory.SCALE,
            ...rest,
        }),
        flower001: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/props.png",
            cordX: 45,
            cordY: 16,
            w: 32 * GameFactory.SCALE,
            h: 46 * GameFactory.SCALE,
            ...rest,
        }),
        flower002: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/props.png",
            cordX: 4,
            cordY: 32,
            w: 32 * GameFactory.SCALE,
            h: 32 * GameFactory.SCALE,
            ...rest,
        }),
        elevatorPanel: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 176,
            cordY: 190,
            w: 16 * GameFactory.SCALE,
            h: 48 * GameFactory.SCALE,
            ...rest,
        }),
        wallClean: ({ x, y, w = 16, h = 16, ...rest } = {}) => ({
            x,
            y,
            w: w * GameFactory.SCALE,
            h: h * GameFactory.SCALE,
            solidFill: "#3b1158",
            ...rest,
        }),
        wallSharpLeftTop: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/walls.png",
            cordX: 16,
            cordY: 64,
            w: 32 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        wallSharpLeft: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/walls.png",
            cordX: 16,
            cordY: 80,
            w: 32 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        wallSharpLeftBottom: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/walls.png",
            cordX: 16,
            cordY: 96,
            w: 32 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        wallSharpLeftCol: ({ x, y, count, ...rest } = {}) =>
            Array.from({ length: count }, (_, i) =>
                GameFactory.environment.wallSharpLeft({
                    x,
                    y: y + i * 16 * GameFactory.SCALE,
                    ...rest,
                }),
            ),
        wallSharpRightTop: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/walls.png",
            cordX: 112,
            cordY: 64,
            w: 32 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        wallSharpRight: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/walls.png",
            cordX: 112,
            cordY: 80,
            w: 32 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        wallBrickLeft: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 9,
            cordY: 356,
            w: 55 * GameFactory.SCALE,
            h: 64 * GameFactory.SCALE,
            ...rest,
        }),
        wallBrickRight: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 112,
            cordY: 356,
            w: 55 * GameFactory.SCALE,
            h: 64 * GameFactory.SCALE,
            ...rest,
        }),
        wallBrickVerticalPattern: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 80,
            cordY: 372,
            w: 16 * GameFactory.SCALE,
            h: 64 * GameFactory.SCALE,
            ...rest,
        }),
        wallBrickLeftSquare: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 16,
            cordY: 411,
            w: 48 * GameFactory.SCALE,
            h: 24 * GameFactory.SCALE,
            ...rest,
        }),
        wallBrickRightSquare: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 112,
            cordY: 411,
            w: 48 * GameFactory.SCALE,
            h: 24 * GameFactory.SCALE,
            ...rest,
        }),
        wallSharpRightBottom: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/walls.png",
            cordX: 112,
            cordY: 96,
            w: 32 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        wallSharpRightCol: ({ x, y, count, ...rest } = {}) =>
            Array.from({ length: count }, (_, i) =>
                GameFactory.environment.wallSharpRight({
                    x,
                    y: y + i * 16 * GameFactory.SCALE,
                    ...rest,
                }),
            ),
        wallSharpHole: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/walls.png",
            cordX: 64,
            cordY: 64,
            w: 32 * GameFactory.SCALE,
            h: 64 * GameFactory.SCALE,
            ...rest,
        }),
        wallInside: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 222,
            cordY: 144,
            w: 16 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        wallPipe: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets.png",
            cordX: 336,
            cordY: 192,
            w: 32 * GameFactory.SCALE,
            h: 32 * GameFactory.SCALE,
            ...rest,
        }),
        electronics: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets-2.png",
            cordX: 32,
            cordY: 128,
            w: 32 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        electronic001: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets-2.png",
            cordX: 32,
            cordY: 128,
            w: 16 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        electronic002: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets-2.png",
            cordX: 48,
            cordY: 128,
            w: 16 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
        electronic003: ({ x, y, ...rest } = {}) => ({
            x,
            y,
            url: "textures/tilesets-2.png",
            cordX: 21,
            cordY: 32,
            w: 16 * GameFactory.SCALE,
            h: 16 * GameFactory.SCALE,
            ...rest,
        }),
    },
    grid: {
        teleport: ({ id, x, y, targetX, targetY, ...rest } = {}) => ({
            id,
            x: x * GameFactory.GRID,
            y: y * GameFactory.GRID,
            w: 96,
            h: 192,
            targetX: targetX * GameFactory.GRID,
            targetY: targetY * GameFactory.GRID,
            delay: 200,
            frozenDelay: 650,
            platform: {
                id: _nextTeleportPlatformId++,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                w: 96,
                h: 48,
                type: "solid",
                layout: "simple",
                color: "transparent",
            },
            targetPlatform: {
                id: _nextTeleportPlatformId++,
                x: targetX * GameFactory.GRID,
                y: targetY * GameFactory.GRID,
                w: 96,
                h: 48,
                type: "solid",
                layout: "simple",
                color: "transparent",
            },
            ...rest,
        }),
        enemy: ({
            id,
            startX,
            targetX,
            startY,
            targetY,
            speed,
            health,
            ...rest
        } = {}) =>
            GameFactory.enemy({
                id,
                startX: startX * GameFactory.GRID,
                targetX: targetX * GameFactory.GRID,
                startY: startY * GameFactory.GRID,
                targetY: targetY * GameFactory.GRID,
                speed,
                health,
                ...rest,
            }),
        slime: ({
            id,
            startX,
            targetX,
            startY,
            targetY,
            speed,
            health,
            ...rest
        } = {}) =>
            GameFactory.slime({
                id,
                startX: startX * GameFactory.GRID,
                targetX: targetX * GameFactory.GRID,
                startY: startY * GameFactory.GRID - 32,
                targetY: targetY * GameFactory.GRID - 32,
                speed,
                health,
                ...rest,
            }),
        evilEye: ({
            id,
            startX,
            targetX,
            startY,
            targetY,
            speed,
            health,
            ...rest
        } = {}) =>
            GameFactory.evilEye({
                id,
                startX: startX * GameFactory.GRID,
                targetX: targetX * GameFactory.GRID,
                startY: startY * GameFactory.GRID,
                targetY: targetY * GameFactory.GRID,
                speed,
                health,
                ...rest,
            }),
        cannon: ({ id, x, y, targetY, ...rest } = {}) =>
            GameFactory.cannon({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                targetY: targetY * GameFactory.GRID,
                ...rest,
            }),
        exit: ({ id, x, y, w = 80, h = 112, ...rest } = {}) =>
            GameFactory.exit({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                w,
                h,
                ...rest,
            }),
        message: ({
            id,
            x,
            y,
            w,
            h,
            offsetX = 0,
            offsetY = 0,
            title,
            lines,
            relatedTo,
            strategy,
            displayTime,
            delay,
        } = {}) =>
            GameFactory.message({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                w: w * GameFactory.GRID,
                h: h * GameFactory.GRID,
                offsetX: offsetX * GameFactory.GRID,
                offsetY: offsetY * GameFactory.GRID,
                title,
                lines,
                relatedTo,
                strategy,
                displayTime,
                delay,
            }),
        solid: ({ id, x, y, w, h, layout = "ground" } = {}) =>
            GameFactory.solid({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                w: w * GameFactory.GRID,
                h: h * GameFactory.GRID,
                layout,
            }),
        ice: ({
            id,
            x,
            y,
            w,
            h,
            layout = "ice",
            acceleration = 0.06,
            deceleration = 0.03,
            airAcceleration = 0.18,
            airDeceleration = 0.09,
        } = {}) =>
            GameFactory.ice({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                w: w * GameFactory.GRID,
                h: h * GameFactory.GRID,
                acceleration,
                deceleration,
                airAcceleration,
                airDeceleration,
                layout,
            }),
        oneDirectionIce: ({
            id,
            x,
            y,
            w,
            h,
            layout = "iceOneDirection",
            acceleration = 0.05,
            deceleration = 0.025,
            airAcceleration = 0.15,
            airDeceleration = 0.075,
        } = {}) =>
            GameFactory.oneDirectionIce({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                w: w * GameFactory.GRID,
                h: h * GameFactory.GRID,
                acceleration,
                deceleration,
                airAcceleration,
                airDeceleration,
                layout,
            }),
        oneDirection: ({
            id,
            x,
            y,
            w,
            h,
            layout = "boardOneDirection",
            ...rest
        } = {}) =>
            GameFactory.oneDirection({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                w: w * GameFactory.GRID,
                h: h * GameFactory.GRID,
                layout,
                ...rest,
            }),
        cracks: ({ id, x, y, w, h, layout = "cracks" } = {}) =>
            GameFactory.cracks({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                w: w * GameFactory.GRID,
                h: h * GameFactory.GRID,
                layout,
            }),
        booster: ({ id, x, y, w, h, boostSpeed = 20 } = {}) =>
            GameFactory.booster({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                w: w * GameFactory.GRID,
                h: h * GameFactory.GRID,
                boostSpeed,
            }),
        elevator: ({
            id,
            startX,
            startY,
            w,
            h,
            targetX,
            targetY,
            speed,
            waitTime,
            layout,
            triggered,
        } = {}) =>
            GameFactory.elevator({
                id,
                startX: startX * GameFactory.GRID,
                startY: startY * GameFactory.GRID,
                w: w * GameFactory.GRID,
                h: h * GameFactory.GRID,
                targetX: targetX * GameFactory.GRID,
                targetY: targetY * GameFactory.GRID,
                speed,
                waitTime,
                layout,
                triggered,
            }),
        coins: ({ id, x, y, ...rest } = {}) =>
            GameFactory.collectible({
                id,
                x: x * GameFactory.GRID + GameFactory.GRID / 4,
                y: y * GameFactory.GRID + GameFactory.GRID / 4,
                ...rest,
            }),
        hearts: ({ id, x, y, ...rest } = {}) =>
            GameFactory.collectible({
                id,
                x: x * GameFactory.GRID + GameFactory.GRID / 4,
                y: y * GameFactory.GRID + GameFactory.GRID / 4,
                ...rest,
            }),
        splinters: ({ id, x, y, ...rest } = {}) =>
            GameFactory.collectible({
                id,
                x: x * GameFactory.GRID + 6,
                y: y * GameFactory.GRID + 6,
                w: 36,
                h: 36,
                ...rest,
            }),
        artifact: ({ id, x, y, cordX, cordY, ...rest } = {}) =>
            GameFactory.artifact({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                cordX,
                cordY,
                ...rest,
            }),
        weaponUpgrades: ({ id, x, y, ...rest } = {}) =>
            GameFactory.collectible({
                id,
                x: x * GameFactory.GRID + 8,
                y: y * GameFactory.GRID + 8,
                w: 32,
                h: 32,
                message: {
                    lines: MESSAGES.WEAPON_UPGRADED,
                    relatedTo: "viewPort",
                    displayTime: 3000,
                },
                ...rest,
            }),
        rowOfCollectibles: ({ startId, count, x, y, gap = 1 } = {}) =>
            GameFactory.rowOfCollectibles({
                startId,
                count,
                startX: x * GameFactory.GRID + GameFactory.GRID / 4,
                y: y * GameFactory.GRID + GameFactory.GRID / 4,
                gap: gap * GameFactory.GRID,
            }),
        columnOfCollectibles: ({ startId, count, x, y, gap = 1 } = {}) =>
            GameFactory.columnOfCollectibles({
                startId,
                count,
                x: x * GameFactory.GRID + GameFactory.GRID / 4,
                startY: y * GameFactory.GRID + GameFactory.GRID / 4,
                gap: gap * GameFactory.GRID,
            }),
        rowOfSpikes: ({
            startId,
            count,
            x,
            y,
            position = "down",
            damage = 1,
        } = {}) =>
            GameFactory.rowOfSpikes({
                startId,
                count,
                startX: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                position,
                damage,
            }),
        spike: ({
            id,
            x,
            y,
            variant = 1,
            position = "down",
            damage = 1,
            offsetX = 0,
            offsetY = 0,
        } = {}) =>
            GameFactory.spike({
                id,
                x: x * GameFactory.GRID,
                y: y * GameFactory.GRID,
                variant,
                position,
                damage,
                offsetX,
                offsetY,
            }),
        checkpoint: ({
            id,
            x,
            y,
            w,
            h,
            reached = false,
            message = {},
        } = {}) => {
            const cp = {
                ...GameFactory.checkpoint({
                    id,
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    w: w * GameFactory.GRID,
                    h: h * GameFactory.GRID,
                    reached,
                }),
                back: GameFactory.environment.checkpointBack({
                    x: x * GameFactory.GRID - 6 * GameFactory.SCALE,
                    y: y * GameFactory.GRID,
                }),
                front: GameFactory.environment.checkpointFront({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                }),
                platform: {
                    id: _nextCheckpointPlatformId++,
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    w: 30,
                    h: 3,
                    type: "solid",
                    layout: "simple",
                    color: "#bd067b",
                },
            };
            if (message !== undefined) {
                const {
                    offsetX = 0,
                    offsetY = -2,
                    lines = MESSAGES.CHECKPOINT,
                    relatedTo = "hitBox",
                    strategy = "single",
                    displayTime = 3000,
                } = message;
                cp.message = GameFactory.message({
                    id: `checkpoint-${id}-msg`,
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    w: w * GameFactory.GRID,
                    h: h * GameFactory.GRID,
                    offsetX: offsetX * GameFactory.GRID,
                    offsetY: offsetY * GameFactory.GRID,
                    lines,
                    relatedTo,
                    strategy,
                    displayTime,
                });
            }
            return cp;
        },
        environment: {
            teleport: ({ x, y, ...rest } = {}) =>
                GameFactory.teleport({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            checkpointBack: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.checkpointBack({
                    x: x * GameFactory.GRID - 6 * GameFactory.SCALE,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            checkpointFront: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.checkpointFront({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            terminalDirection: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.terminalDirection({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            terminal: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.terminal({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            plant001: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.plant001({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 14 * GameFactory.SCALE,
                    ...rest,
                }),
            plant002: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.plant002({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 12 * GameFactory.SCALE,
                    ...rest,
                }),
            plant003: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.plant003({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 7 * GameFactory.SCALE,
                    ...rest,
                }),
            plant004: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.plant004({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 8 * GameFactory.SCALE,
                    ...rest,
                }),
            plant005: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.plant005({
                    x: x * GameFactory.GRID + 2 * GameFactory.SCALE,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            plant006: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.plant006({
                    x: x * GameFactory.GRID - 2 * GameFactory.SCALE,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            plant007: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.plant007({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 1 * GameFactory.SCALE,
                    ...rest,
                }),
            plant008: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.plant008({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 1 * GameFactory.SCALE,
                    ...rest,
                }),
            wallPlant001: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallPlant001({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            plate001: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.plate001({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 7 * GameFactory.SCALE,
                    ...rest,
                }),
            flower001: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.flower001({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 14 * GameFactory.SCALE,
                    ...rest,
                }),
            flower002: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.flower002({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 14 * GameFactory.SCALE,
                    ...rest,
                }),
            elevatorPanel: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.elevatorPanel({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID - 16 * GameFactory.SCALE,
                    ...rest,
                }),
            wallClean: ({ x, y, w = 1, h = 1, ...rest } = {}) =>
                GameFactory.environment.wallClean({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    w: (w * GameFactory.GRID) / GameFactory.SCALE,
                    h: (h * GameFactory.GRID) / GameFactory.SCALE,
                    ...rest,
                }),
            wallSharpLeftTop: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallSharpLeftTop({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallSharpLeft: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallSharpLeft({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallSharpLeftBottom: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallSharpLeftBottom({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallSharpLeftCol: ({ x, y, count, ...rest } = {}) =>
                GameFactory.environment.wallSharpLeftCol({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    count,
                    ...rest,
                }),
            wallSharpRightTop: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallSharpRightTop({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallSharpRight: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallSharpRight({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallSharpRightBottom: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallSharpRightBottom({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallSharpRightCol: ({ x, y, count, ...rest } = {}) =>
                GameFactory.environment.wallSharpRightCol({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    count,
                    ...rest,
                }),
            wallSharpHole: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallSharpHole({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallInside: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallInside({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallBrickLeft: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallBrickLeft({
                    x: x * GameFactory.GRID - 21,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallBrickRight: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallBrickRight({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallBrickVerticalPattern: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallBrickVerticalPattern({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallBrickLeftSquare: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallBrickLeftSquare({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallBrickRightSquare: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallBrickRightSquare({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            wallPipe: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.wallPipe({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            electronics: ({ x, y, w, h, ...rest } = {}) =>
                GameFactory.environment.electronics({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    repeatX: Math.ceil(w / 2),
                    repeatY: h,
                    ...rest,
                }),
            electronic001: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.electronic001({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            electronic002: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.electronic002({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
            electronic003: ({ x, y, ...rest } = {}) =>
                GameFactory.environment.electronic003({
                    x: x * GameFactory.GRID,
                    y: y * GameFactory.GRID,
                    ...rest,
                }),
        },
    },
};
